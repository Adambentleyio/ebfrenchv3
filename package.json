{
  "name": "ebfrench-tutoring",
  "version": "3.0.0",
  "private": true,
  "description": "EB French Tutoring — static marketing site",
  "scripts": {
    "build": "npx tailwindcss -i ./css/input.css -o ./css/styles.css --minify",
    "watch": "npx tailwindcss -i ./css/input.css -o ./css/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
